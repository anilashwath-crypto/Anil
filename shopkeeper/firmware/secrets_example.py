"""
Copy to secrets.py and fill in. secrets.py is gitignored and never committed.

    JOIN = ("your-ssid", "your-password")

Leave JOIN as None to skip joining a network entirely — the cabinet then always
brings up its own access point, which is what it does at a demo anyway.

AP_PASSWORD is required (8+ chars, WPA2). Pick a unique one per cabinet.
"""
JOIN = None
AP_PASSWORD = "change-me-per-unit"
